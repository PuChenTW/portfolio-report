[project]
name = "portfolio-core"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["yfinance>=1.2.2", "pyyaml>=6.0"]

[tool.setuptools.packages.find]
include = ["portfolio"]

[dependency-groups]
dev = ["pytest>=9.0.3"]
