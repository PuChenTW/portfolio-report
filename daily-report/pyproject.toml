[project]
name = "daily-report"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "portfolio-core",
    "pydantic-ai>=0.4.0",
    "tavily-python>=0.5.0",
]

[tool.uv.sources]
portfolio-core = { workspace = true }

[tool.setuptools.packages.find]
include = ["pipeline"]
